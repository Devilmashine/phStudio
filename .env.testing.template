# Testing Environment Configuration
# This file is used for running tests with PostgreSQL

# === Environment ===
ENV=testing

# === Database Configuration ===
# Use separate test database to avoid conflicts with development data
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/phstudio_test
TEST_DATABASE_URL=postgresql://postgres:postgres@localhost:5432/phstudio_test

# Database Connection Pool Settings (Testing - smaller pools)
DB_POOL_SIZE=5
DB_POOL_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=10
DB_POOL_RECYCLE=1800

# === Security (Testing) ===
SECRET_KEY=test-secret-key-not-for-production
ACCESS_TOKEN_EXPIRE_MINUTES=5
REFRESH_TOKEN_EXPIRE_MINUTES=10
ALGORITHM=HS256

# === CORS Configuration ===
CORS_ORIGINS_STR=http://localhost:3000,http://localhost:5173

# === Telegram Integration (Test) ===
TELEGRAM_BOT_TOKEN=test_token
TELEGRAM_CHAT_ID=test_chat_id

# === Redis Configuration (Test) ===
REDIS_URL=redis://localhost:6379/1

# === Logging ===
LOG_LEVEL=WARNING

# === Test-specific Settings ===
ENABLE_DB_LOGGING=false
DISABLE_TELEGRAM_NOTIFICATIONS=true
TEST_MODE=true

# === URLs for Testing ===
FRONTEND_URL=http://localhost:3000
BACKEND_URL=http://localhost:8888